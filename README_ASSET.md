# HiF4 GPTQ implementation asset v4.9.2-gptq

This source-only asset contains the repository-style, blockwise column GPTQ implementation,
HiF4 format semantics, curvature representation, Wan cache builder, and checksums.
It intentionally excludes checkpoints, calibration tensors, generated caches, and model data.

Validate after extraction:

```bash
sha256sum -c SHA256SUMS
```
